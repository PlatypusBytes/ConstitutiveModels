name: Tests

# Controls when the workflow will run
on:
  pull_request:
    branches: [ "**" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
#        os: [ubuntu-latest, windows-latest]
        os: [windows-latest]
        python-version: [' 3.10', '3.11', '3.12']

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    - uses: actions/checkout@v3

    - name: Setup VS Developer Command Prompt
      uses: ilammy/msvc-dev-cmd@v1
      with:
        arch: x64

    - name: Build C DLL
      run: |
        call compile_c_models.bat
      shell: cmd

#    - name: Upload DLL artifact
#      uses: actions/upload-artifact@v3
#      with:
#        name: linear_elastic_dll
#        path: c_models/linear_elastic_with_vertical_tens_cutoff/linear_elastic_with_vertical_tens_cutoff.dll




#      run: call c_models/linear_elastic_with_vertical_tens_cutoff/compile.bat


#    - name: Install libGLU library on Ubuntu
#      run: |
#            if [ "$RUNNER_OS" == "Linux" ]; then
#                sudo apt-get install -y libglu1-mesa
#            fi
#      shell: bash
    - uses: actions/checkout@v3
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Test with pytest
      run: pytest tests

